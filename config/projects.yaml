# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json
#
# Contenu de la page Projets, en configuration pour l'instant — comme
# config/hub.yaml. Migrera en base de données quand l'espace Admin sera
# développé (les projets y seront alors ajoutables directement).
parameters:
    # status : 'in_progress' | 'done' | 'archived'
    app.projects:
        - name: 'PetrosLabs'
          summary:
              fr: "Ce site : un hub Symfony reliant blog, projets et réseaux, avec une direction artistique entre Grèce antique et laboratoire de dev moderne."
              en: 'This site: a Symfony hub linking blog, projects and social links, with an art direction between ancient Greece and a modern dev lab.'
          image: 'images/projects/project-1.webp'
          stack: ['Symfony', 'Tailwind CSS v4', 'AssetMapper']
          status: 'in_progress'
          repo_url: 'https://github.com/petroslabs/portfolio'
          demo_url: ~
