{# Pied de page commun, réutilisé sur toutes les pages #}
{% props class = '' %}
<footer class="mt-auto text-center {{ class }}">
    <p class="font-mono text-xs text-parchment-dim/70">
        © {{ "now"|date("Y") }} PetrosLabs — {{ 'footer.tagline'|trans }}.
    </p>
</footer>
